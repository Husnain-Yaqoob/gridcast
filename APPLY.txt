Copy what is INSIDE this folder over your project.

  gridcast\api.py     typed response models added
  (everything else unchanged from the last zip)

  python -m pytest              (expect 113 passed)
  python -m gridcast serve

Then refresh http://127.0.0.1:8000/docs - the Responses section now shows
real field names instead of additionalProp1.

Supported by EirGrid Group Data
